# Base URL for REST API (optional — default in vite.config proxy is localhost:4000)
# VITE_API_URL=http://localhost:4000

# Socket.IO connects here (same host/port as API by default)
VITE_SOCKET_URL=http://localhost:4000
